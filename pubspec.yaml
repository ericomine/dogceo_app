name: dogceo_app
description: A new Flutter project.

publish_to: 'none' 
version: 1.0.0+1

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  dio: ^4.0.0


dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^2.0.1
  lint: ^1.5.3
  mockito: ^5.0.5

flutter:
  uses-material-design: true

        
    
    
                                      